function porcentagem(n1, n2) {
    if(porcentagem) {
       let pct = (n2 / n1) * 100;
        return pct;
    } else {
        return pct;
    }
} 

let x = 10;
let y = 20;
let pct = porcentagem(x, y);

console.log(pct)

// Resolução: function porcentagem(n1, n2) {
//     let pct = (n2 / n1) * 100;
//     return pct;
//  } 
 
//  let x = 10;
//  let y = 20;
//  let pct = porcentagem(x, y);
 
//  console.log(pct)